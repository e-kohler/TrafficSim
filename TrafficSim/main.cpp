#include <iostream>
#include <stdlib.h>
#include <time.h>

#include "road.h"
#include "semaphore.h"
#include "../structures/linked_list.h"
#include "../structures/array_list.h"

int main () {
    int freq = 15;
    wayIn O1leste = wayIn(10);
    wayIn N1sul = wayIn(20);
    wayIn O1leste = wayIn(12);
    wayIn O1leste = wayIn(12);
    wayIn O1leste = wayIn(12);
    wayIn O1leste = wayIn(12);


    Semaphore sem1 = Semaphore(freq, 0.1, 0.8, )
	return 0;
}
